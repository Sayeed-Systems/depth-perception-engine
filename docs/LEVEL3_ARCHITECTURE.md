# Level 3 architecture report (Phase E1)

> **Update (2026-08-07, Phase E7 — LEVEL 3 FROZEN):** integrated validation and freeze — no architecture change. Synthetic ground-truth acceptance (6 scenarios, hand-computed expected values, `tests/test_e7_synthetic_ground_truth.py`) and real-hardware validation (a real calibrated USB stereo rig, 7 phases, 336 captured real frames across two sessions) both passed. The UNKNOWN-space safety invariant — the single most important property this whole chain exists to guarantee — was directly re-verified on real sensor data, not just synthetic fixtures: in every real captured frame, `valid_mask.sum() == ObstacleCloud.points.shape[0] == FreeSpaceRays.ranges_m.shape[0]` exactly, with zero exceptions. A standalone visualization tool (`examples/visualize_level3.py`, optional `viz` extra, never imported by core) renders the full chain with an explicit BODY-frame axes triad matching `docs/COORDINATE_FRAMES.md` exactly — no axis flip anywhere (`mplot3d` imposes no convention of its own, verified). Full results, including one specific inconclusive finding (a global nearest-surface scalar didn't cleanly track a moved object in this test environment, root-caused to a persistent nearby surface, not an engine defect) reported honestly rather than smoothed over: `docs/VALIDATION_REPORT.md`'s E7 addendum, including the final acceptance matrix. **This is a validation/documentation pass only** — zero changes to any stereo/depth/point-cloud/transform/obstacle/free-space/metrics code, zero new fields, zero public API changes.

> **Update (2026-08-06, Phase E6):** hardening pass over E2-E5 — no architecture change, no new stage in the geometry chain. Added: `geometry.GeometryQuality`/`classify_geometry_quality()` (`geometry/geometry_metrics.py`), a pure opt-in classifier mapping `GeometryMetrics.valid_fraction` onto `HEALTHY`/`DEGRADED`/`NO_USABLE_GEOMETRY` — plain string constants, matching `obstacles.ThreatAssessor`'s existing `CLEAR`/`CAUTION`/`BLOCKED`/`NO_DATA` precedent rather than introducing a new Enum type; not wired into `process()` or stored on `DepthPerceptionResult`. Two new `PipelineConfig` thresholds (`geometry_healthy_min_valid_fraction`, `geometry_degraded_min_valid_fraction`) feed it. No other production code changed except test-list hygiene (`tests/test_public_api.py`'s `INTERNAL_SYMBOLS` regression list, stale since E2, updated to actually cover `PointCloudBuilder`/`transform_point_cloud`/`build_obstacle_cloud`/`build_free_space_rays`/`build_geometry_metrics`).
>
> **Key audit finding (Task 1/4):** `PipelineHealth` cannot, and was never meant to, express per-frame degradation state — its own pre-existing docstring already says "not a per-frame diagnosis." `DepthPerceptionResult` (via `confidence`, `geometry_metrics`, `traversability_mask`) already carries everything needed; the mission's 5-category taxonomy (VALID/DEGRADED/UNKNOWN/INVALID-INPUT/INTERNAL-FAILURE) requires **zero new frozen contract** — this is a documentation-and-one-small-helper pass, not a redesign. No STOP was triggered.
>
> **Failure semantics, confirmed not changed:** `process()` already had zero `try/except` around any Level 3 stage before E6 — this pass added tests proving that atomicity (`tests/test_failure_containment.py`), it did not add or remove any exception handling.
>
> Six new test files (`tests/test_geometry_quality.py`, `test_adversarial_geometry.py`, `test_failure_containment.py`, `test_determinism.py`, `test_state_recovery.py`, `test_performance_guards.py`) plus two new benchmark scripts (`examples/benchmark_e6_full_pipeline.py`, `examples/benchmark_e6_memory_stability.py`). See `docs/IMPLEMENTATION_STATUS.md`'s E6 addendum and `docs/VALIDATION_REPORT.md`'s E6 addendum for the full results. `ObstacleCloud`/`FreeSpaceRays`/`GeometryMetrics` extension, occupancy, temporal fusion, vehicle envelope remain **not built** — see `docs/E7_IMPLEMENTATION_PLAN.md` (the E5-era `docs/E6_IMPLEMENTATION_PLAN.md`'s speculation about occupancy/temporal fusion did not become this pass's actual scope; carried forward there instead).

> **Update (2026-08-06, Phase E5):** `ObstacleCloud`/`FreeSpaceRays`/`GeometryMetrics` are now built — `geometry/obstacle_extractor.py`'s `build_obstacle_cloud()`, `geometry/free_space.py`'s `build_free_space_rays()`, `geometry/geometry_metrics.py`'s `build_geometry_metrics()`. All three are plain, stateless functions (not classes — unlike `PointCloudBuilder`, none of them hold construction-time state; matches `transform_point_cloud`'s precedent, not `PointCloudBuilder`'s), operating **only** on the already-verified E4 body-frame `PointCloud` — no disparity/depth recomputation, no reprojection, no second camera→body transform (the `origin` each takes is read directly off `self._body_T_camera_left.translation`, never recomputed). Wired into `DepthPerceptionPipeline` behind two new independent flags, `PipelineConfig.enable_obstacle_geometry`/`enable_free_space_rays` (both default `False`), gated *underneath* `enable_geometry` + a supplied `body_T_camera_left` (E5 has nothing to build from otherwise). `DepthPerceptionResult` gained three more additive fields at the very end: `obstacle_cloud`, `free_space_rays`, `geometry_metrics` — same "reuse the frozen type, don't invent a composite" reasoning as E3/E4.
>
> **Mismatches found and resolved (Task 1's audit, reported rather than silently resolved):**
> 1. The E5 task prose asked for a `timestamp` field on `ObstacleCloud`/`FreeSpaceRays` — neither frozen type has one (confirmed against `geometry/types.py`, unchanged since E1). Resolved by using the frozen 4-field/4-field shapes exactly, with no new field added; the frame's timestamp remains available via `geometry_body.timestamp` and `DepthPerceptionResult.timestamp`.
> 2. The E5 task prose describes `FreeSpaceRays` as `[origin, endpoint)` pairs — the frozen type stores `(origins, directions, ranges_m)` instead (a parametrization, not an endpoint list). Mathematically equivalent (`endpoint = origin + direction * range`, verified directly in `tests/test_free_space.py`); built the frozen parametrization, not a new `endpoints` field.
> 3. `GeometryMetrics`'s frozen 4 fields (`min_obstacle_distance_m`, `mean_free_space_m`, `point_count`, `valid_fraction`) are narrower than the task's illustrative metric wishlist (max/farthest distance, separate obstacle/ray counts, unknown_fraction, spatial coverage). Populated exactly the frozen 4 fields, each with one precise definition (`geometry/geometry_metrics.py`'s docstring); the rest are flagged as a genuine E6 candidate, not silently added to the frozen type.
>
> **A real correctness bug was found and fixed during this pass**, not merely a documentation gap: both builders' first draft admitted a contract-violating Inf-valued-but-`valid_mask=True` point when `obstacle_max_range_m` was left at its actual default (`+inf`) — `inf <= inf` is `True` in IEEE-754, so the range check alone did not exclude it, and `Inf / Inf` produces a `NaN` direction in `build_free_space_rays`. Fixed by adding an explicit `np.isfinite(...)` guard in both functions, independent of the configured range bounds — caught by this pass's own `tests/test_obstacle_extractor.py::TestNaNInfNeverBecomeObstacles`/`tests/test_free_space.py::TestInvalidPointsGenerateNoRay` before being wired into the pipeline. See `docs/VALIDATION_REPORT.md`'s E5 addendum.
>
> **Core safety rule enforcement:** every valid pixel produces exactly one obstacle-candidate point (subject to range/stride) and exactly one free-space ray (subject to stride only — range filtering is obstacle-specific, per the task's own `obstacle_min_range_m`/`obstacle_max_range_m` naming); every invalid pixel produces neither. This is a structural guarantee (both builders derive their selection mask from `cloud.valid_mask` directly, ANDed with, never ORed with, the range check), not a post-hoc filter — verified in `tests/test_pipeline_spatial_evidence.py::TestUnknownSpaceSafetyRule`.
>
> `ObstacleAssessment` (`DepthPerceptionResult.obstacles`, Level 0-2's existing 2D per-beam scan) is untouched and unrelated to the new `obstacle_cloud` field — different concept (2D beam-percentile scan vs. 3D filtered point set), different vintage, explicitly not merged or renamed.
>
> Still **not built**: occupancy/voxel mapping, temporal fusion, vehicle envelope, collision-risk scoring — E6+, see `docs/E6_IMPLEMENTATION_PLAN.md`. **E5 does not produce an occupancy map** — `FreeSpaceRays` are single-frame observations suitable for a future occupancy consumer to integrate, not an integrated map themselves.

> **Update (2026-08-06, Phase E4):** body-frame transformation is now built — `geometry/rigid_transform.py`'s `transform_point_cloud(cloud, transform)`, wired into `DepthPerceptionPipeline` behind a new optional `body_T_camera_left` constructor parameter (defaults `None` — see `docs/COORDINATE_FRAMES.md`). `DepthPerceptionResult.geometry_body: Optional[PointCloud] = None` is the new result field, additive at the very end (after E3's `geometry`), reusing `PointCloud` directly again — same "reuse the frozen type, don't invent a composite" reasoning as E3. `ObstacleCloud`/`FreeSpaceRays`/`GeometryMetrics`/occupancy remain **not built** — E5+, see `docs/E5_IMPLEMENTATION_PLAN.md`. One naming clarification worth stating plainly: E1's `RigCalibration.body_T_camera_left` field already existed as a place to *carry* this transform alongside a `StereoCalibration`; E4 did not construct a `RigCalibration` inside the pipeline's public surface (that would require accepting a `RigCalibration` instead of a plain `StereoCalibration`, changing the frozen constructor's second-argument type — a real breaking change, not attempted) — instead `DepthPerceptionPipeline.__init__` accepts the `RigidTransform` directly as a new, additive, defaulted parameter, and internally constructs a throwaway `RigCalibration` purely to reuse its already-frozen `__post_init__` validation (`to_frame == BODY`, `from_frame == camera_frame_id`) without duplicating that logic. See `docs/IMPLEMENTATION_STATUS.md`'s E4 addendum for the full record.

> **Update (2026-08-06, Phase E3):** the "Future, E2+, not built in this pass" section below is now partly out of date — `geometry/point_cloud_builder.py`/`PointCloudBuilder` was built and validated in E2, and in this E3 pass was wired into `DepthPerceptionPipeline.process()` behind `PipelineConfig.enable_geometry` (default `False`). `obstacle_extractor.py`/`free_space.py` remain **not built** — still E4+; no separate E3 architecture doc exists, this update plus `docs/IMPLEMENTATION_STATUS.md`'s E3 addendum are the record. Two E1-pass decisions from this document were revisited in E3 and are worth flagging explicitly:
>
> 1. **`pipeline/pipeline.py` is no longer "UNCHANGED."** E1 listed it as untouched by design (E1 was contracts-only). E3's whole mission was wiring geometry into it — `DepthPerceptionPipeline.__init__`/`.process()` now conditionally build/call `PointCloudBuilder`, gated by `enable_geometry` and reproducing byte-identical Level 0-2 output when `False` (see `tests/test_pipeline_geometry.py::TestZeroRegression`).
> 2. **`DepthPerceptionResult.geometry` is typed `Optional[geometry.PointCloud]` directly, not the `GeometryResult` composite `docs/E2_IMPLEMENTATION_PLAN.md` sketched.** That composite (bundling `PointCloud`/`ObstacleCloud`/`FreeSpaceRays`/`GeometryMetrics`) was explicitly marked "exact shape TBD... not frozen by E1" in that planning doc — not an E1 contract. Since E3's scope produces only `PointCloud` (`ObstacleCloud`/`FreeSpaceRays` remain E4+, per this task's own non-goals), inventing a composite wrapper for three types that don't exist yet would itself be new, undocumented API surface. Reusing the actual frozen `PointCloud` type directly is the smaller, non-inventive change; a `GeometryResult` composite (if still wanted once `ObstacleCloud`/`FreeSpaceRays` are real) is deferred to whichever phase actually builds those.

## Current coordinate system, as found

Before this pass: entirely implicit. `DepthEstimator.estimate()`/`estimate_point_cloud()` operate in the rectified left camera's optical frame (OpenCV convention: X right, Y down, Z forward, right-handed) purely because that's what `cv2.reprojectImageTo3D`/the calibration's `Q` matrix already assume — nothing in this repository ever states this in code, a docstring, or a doc file. No body frame, no rigid-transform type, no frame-naming convention, no camera-model concept existed anywhere (confirmed by direct grep across `src/` this pass: zero hits for frame/handedness/body-frame/camera-model terms).

## Problems discovered

1. **Implicit, unnamed coordinate frame.** A future consumer of `depth_map` or a point cloud has no way to know which frame the values are in without reading source comments — and even those comments (added in the previous session's `docs/DATA_CONTRACTS.md`) were an inference from OpenCV's known conventions, not a stated, validated contract.
2. **No body-frame relationship exists at all.** Blocks any future vehicle-relative geometry (clearance, collision volume — Level 4+) until someone defines a convention; nothing to build on, not even a placeholder.
3. **`StereoCalibration` conflates intrinsic + extrinsic (left-right) + rectification into one flat bag.** Baseline/focal-length are only derivable ad hoc, buried as private state inside `DepthEstimator.__init__` — not independently testable or reusable by anything else that might need them (e.g. a future geometry module).
4. **`DepthEstimator.estimate_point_cloud()` is dead code.** Found in the previous session's perception capability audit: real implementation, zero callers, zero tests, no frame identity, no validity/confidence attachment. Confirmed still true this pass — not touched, since E1 explicitly forbids implementing geometry.
5. **`StereoObservation` had no reserved extensibility point for calibration.** A future multi-rig/multi-camera caller had nowhere to attach per-observation calibration without changing `process_observation()`'s signature.
6. **No geometry result types existed at all** — Level 3 work would otherwise start by inventing shapes/units/ownership conventions ad hoc, mid-implementation, rather than against a reviewed contract.

## Proposed architecture

Additive-only, layered on top of the untouched Level 0–2 pipeline:

```
src/depth_perception_engine/
├── frames.py                 # NEW — FrameId, RigidTransform (naming + transform representation)
├── calibration/
│   ├── models.py             # UNCHANGED — StereoCalibration, still what Rectification/DepthEstimator consume
│   ├── contracts.py          # NEW — CameraModel, CameraIntrinsics, StereoExtrinsics,
│   │                         #        RectificationParameters, RigCalibration (all derived views)
│   └── loader.py             # UNCHANGED
├── geometry/                 # NEW package — interfaces only
│   ├── __init__.py
│   └── types.py              # PointCloud, ObstacleCloud, FreeSpaceRays, GeometryMetrics
├── models/result.py          # StereoObservation gains one Optional field (calibration); nothing else changes
└── pipeline/                 # UNCHANGED
```

Future, E2+, **not built in this pass** (E1) — status as of E3, 2026-08-06:
```
geometry/
├── point_cloud_builder.py    # BUILT (E2) + WIRED INTO pipeline.py (E3, gated by enable_geometry)
├── rigid_transform.py        # BUILT (E4) + WIRED INTO pipeline.py (E4, gated by body_T_camera_left)
├── obstacle_extractor.py     # BUILT (E5) + WIRED INTO pipeline.py (E5, gated by enable_obstacle_geometry)
├── free_space.py             # BUILT (E5) + WIRED INTO pipeline.py (E5, gated by enable_free_space_rays)
└── geometry_metrics.py       # BUILT (E5) + WIRED INTO pipeline.py (E5, no separate gate)
```

## Reasoning

- **Additive-only mirrors this session's established precedent.** The previous session's baseline recovery pass (lifecycle API, `StereoObservation`, validity masks) proved this pattern works: every new field/type defaults to inert/`None`, every existing call site keeps working unmodified, `mp01_perception` requires zero changes. E1 follows the identical discipline.
- **`StereoCalibration` itself stays untouched** rather than being decomposed in place, specifically because `RectificationEngine`/`DepthEstimator`/`calibration/loader.py` consume its exact current shape — changing it would count as modifying the processing pipeline, which E1 explicitly forbids, even if the change were shape-only with no math difference. Read-only derived views (`.from_calibration(...)`) achieve the same "distinguish intrinsic/extrinsic/rectification" goal with zero risk to the tested, hardware-verified consumers.
- **Geometry types live in a new top-level `geometry/` package, not inside `models/`,** because they represent a genuinely distinct future capability tier (Level 3), not more output shapes for the existing Level 0–2 pipeline — keeping them physically separate makes "this is not real yet" legible from the directory structure alone, and matches the existing precedent of one subpackage per capability area (`traversability/`, `obstacles/`, `quality/`).
- **Nothing new is exported from the top-level `depth_perception_engine` package.** Promoting unproduced contracts to the top-level namespace would overclaim capability (a caller seeing `depth_perception_engine.PointCloud` would reasonably assume something produces one) and contradicts the encapsulation goal this task itself asks for — see `docs/LEVEL3_PUBLIC_API.md`.

## What this pass explicitly did not do

Implement Level 3 geometry, point cloud generation, obstacle extraction, occupancy mapping, IMU integration, temporal fusion, ROS integration, or any change to `stereo/`, `depth/depth_estimator.py`'s existing methods, `traversability/`, `obstacles/`, `fusion/`, `pipeline/pipeline.py`, `config/pipeline_config.py`, or `mp01_perception`. Verified: `pytest tests/ -q` — 162 passed (131 before this pass, +31 new contract tests, zero existing test modified in a way that changes its assertions), confirming the existing Level 0–2 behavior is byte-for-byte unchanged.
