"""D2 — observation identity contract: focused A/B performance check.

Deliberately NOT a repeat of the D1 benchmark campaign. It answers exactly
one question: does copying an opaque identity string through the pipeline
cost anything measurable? Nothing here touches production code.
"""
